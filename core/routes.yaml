apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: argocd-server-tls
  namespace: argocd
spec:
  parentRefs:
  - name: gateway-internal
    namespace: gateway-apis
    sectionName: http
  hostnames:
  - argocd.int.midugh.fr
  rules:
  - filters:
    - type: RequestRedirect
      requestRedirect:
        scheme: https
        statusCode: 301
--- 
apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: argocd-server
  namespace: argocd
spec:
  parentRefs:
  - name: gateway-internal
    namespace: gateway-apis
    sectionName: https
  hostnames:
  - argocd.int.midugh.fr
  rules:
  - matches:
    - path:
        value: /
        type: PathPrefix
    backendRefs:
    - name: argocd-server
      port: 80
